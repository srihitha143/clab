#include<stdio.h>
#include<limits.h>
main()
{
    printf("maximum for integer is %d",INT_MAX);
	printf("minimum for integer is %d",INT_MIN);
	printf("maximum for float is %f",INT_MAX);
	printf("minimum for float is %f",INT_MIN);
    printf("maximum for char is %c",INT_MAX);
	printf("minimum for char is %c",INT_MIN);
    printf("maximum for long is %ld",INT_MAX);
	printf("minimum for long is %ld",INT_MIN);
}
